To build your Windows installer:

1. Replace build/icon.ico with your real app icon (256x256 .ico recommended).
2. Run: npm run dist
3. The installer will be in the dist/ folder as a .exe file.
4. The installer will prompt for installation directory, and create desktop/start menu shortcuts.
